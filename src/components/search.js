import { Link } from "gatsby"
import React from "react"

const Search = () => (
    
    <input
        type="text"
        placeholder="검색해보세요!"
        onChange={handleInputChange}
    />
)



export default Search
